language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t mfnasher/docker-react -f Dockerfile.dev . 

script:
  - docker run -e CI=true mfnasher/docker-react npm run test